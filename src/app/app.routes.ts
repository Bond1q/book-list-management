import { Routes } from '@angular/router';
import { BookListPageComponent } from './pages/book-list/book-list-page.component';

export const routes: Routes = [{ path: '', component: BookListPageComponent }];
